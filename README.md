1. A list of the built-in objects and methods you used
   for Date section : getDate() , getmonth(), getFullYear()
   Number section : Number() , Number.isNaN(), Number.isInteger()
   Number formatting : .toFixed()
   DOM - domcument.getElementByID() , .innerHTML
2. Your **GitHub Pages link**
   https://andyp0nce.github.io/comp484_hw9/
3. Path to a screenshot file of the finished webpage
   /Users/andrewponce/Documents/GitHub/comp484_hw9/images
4. A short reflection of **4-5 sentences** that answers:
   - What part was easiest?
      The easiest part was doing the adding and math part of it. Also the date display was straight forword with the instructions
   - What part was hardest?
      Hardest part was doing the condictions and some of the number conversion, like figuring out if they actually changed the value of the variable or not.
   - What did you learn about the `Date` object?
      Any month after before November has to have a zero infront of it, never thought about it under it required me to put a zero 
   - What did you learn about the `Number` object?
      when convertign a non numeric object, you can use Number(var name) turning it into NaN and seeing if its a number or not
   - What did you learn about displaying results in the browser?
      you have to use document.getElementById(" id name from html ").textContent = result var name ; if you want to display anything from js to the html page
